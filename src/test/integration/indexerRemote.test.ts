//import { createHelia } from 'helia'
//import { json } from '@helia/json'

describe('Test remote DDO', () => {
    it('Upload json to IPFS', async () => {
        /*const helia = await createHelia()
        const j = json(helia)
        const ddress = await j.add({ ddo: 'test' })
        console.log(address, await j.get(address))
        */
    })
})
